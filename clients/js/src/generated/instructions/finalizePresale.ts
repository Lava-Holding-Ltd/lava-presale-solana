/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { LAVA_PROGRAMS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const FINALIZE_PRESALE_DISCRIMINATOR = new Uint8Array([
  100, 245, 60, 81, 177, 232, 255, 99,
]);

export function getFinalizePresaleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_PRESALE_DISCRIMINATOR
  );
}

export type FinalizePresaleInstruction<
  TProgram extends string = typeof LAVA_PROGRAMS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPresaleConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPresaleConfig extends string
        ? WritableAccount<TAccountPresaleConfig>
        : TAccountPresaleConfig,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizePresaleInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizePresaleInstructionDataArgs = {};

export function getFinalizePresaleInstructionDataEncoder(): FixedSizeEncoder<FinalizePresaleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FINALIZE_PRESALE_DISCRIMINATOR })
  );
}

export function getFinalizePresaleInstructionDataDecoder(): FixedSizeDecoder<FinalizePresaleInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizePresaleInstructionDataCodec(): FixedSizeCodec<
  FinalizePresaleInstructionDataArgs,
  FinalizePresaleInstructionData
> {
  return combineCodec(
    getFinalizePresaleInstructionDataEncoder(),
    getFinalizePresaleInstructionDataDecoder()
  );
}

export type FinalizePresaleAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPresaleConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  presaleConfig?: Address<TAccountPresaleConfig>;
};

export async function getFinalizePresaleInstructionAsync<
  TAccountAuthority extends string,
  TAccountPresaleConfig extends string,
  TProgramAddress extends Address = typeof LAVA_PROGRAMS_PROGRAM_ADDRESS,
>(
  input: FinalizePresaleAsyncInput<TAccountAuthority, TAccountPresaleConfig>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  FinalizePresaleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPresaleConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LAVA_PROGRAMS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    presaleConfig: { value: input.presaleConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.presaleConfig.value) {
    accounts.presaleConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 114, 101, 115, 97, 108, 101])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.presaleConfig),
    ],
    data: getFinalizePresaleInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizePresaleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPresaleConfig
  >);
}

export type FinalizePresaleInput<
  TAccountAuthority extends string = string,
  TAccountPresaleConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  presaleConfig: Address<TAccountPresaleConfig>;
};

export function getFinalizePresaleInstruction<
  TAccountAuthority extends string,
  TAccountPresaleConfig extends string,
  TProgramAddress extends Address = typeof LAVA_PROGRAMS_PROGRAM_ADDRESS,
>(
  input: FinalizePresaleInput<TAccountAuthority, TAccountPresaleConfig>,
  config?: { programAddress?: TProgramAddress }
): FinalizePresaleInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountPresaleConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LAVA_PROGRAMS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    presaleConfig: { value: input.presaleConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.presaleConfig),
    ],
    data: getFinalizePresaleInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizePresaleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPresaleConfig
  >);
}

export type ParsedFinalizePresaleInstruction<
  TProgram extends string = typeof LAVA_PROGRAMS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    presaleConfig: TAccountMetas[1];
  };
  data: FinalizePresaleInstructionData;
};

export function parseFinalizePresaleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedFinalizePresaleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), presaleConfig: getNextAccount() },
    data: getFinalizePresaleInstructionDataDecoder().decode(instruction.data),
  };
}
